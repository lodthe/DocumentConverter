language: python

os:
  - linux

python:
  - "3.7"
  - "3.8"

install:
  - pip install -r requirements.txt
  - pip install -r requirements-dev.txt

script:
  - pytest

env:
  - PYTHONPATH=.

notifications:
  email: false